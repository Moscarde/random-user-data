interface Person {
    name: string;
    email: string;
    age: number;
    birthDate: string;
}
export declare function randomUser(genre?: string, minAge?: number, maxAge?: number): Person;
export {};
